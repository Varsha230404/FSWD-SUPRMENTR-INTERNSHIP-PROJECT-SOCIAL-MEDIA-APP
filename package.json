{
  "name": "vibe-social-app",
  "version": "1.0.0",
  "description": "Vibe — a full-stack social media application",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  }
}
